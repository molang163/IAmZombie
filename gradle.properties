# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=26.2
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[26.2]
# The Neo version must agree with the Minecraft version to get a valid artifact.
# Certified baseline for compilation, automated tests, dedicated-server startup,
# client world entry, and the first-person rendering matrix.
neo_version=26.2.0.25-beta
# Supported NeoForge range for the shipped jar's dependency declaration.
# Lower bound 26.2.0.12-beta is the retained MINIMUM supported version: its binary
# compatibility is proven by running the SAME .25-built jar on a .12 instance (not a
# .12 recompile). Upper bound 26.2.1-alpha (exclusive) allows every later 26.2.0.x on
# the same MC 26.2 line as expected forward-compat (issues accepted as feedback, not
# separately certified), while excluding the ENTIRE next Minecraft line. Bounding at the
# release 26.2.1 would still admit its prereleases (26.2.1-alpha / 26.2.1-beta /
# 26.2.1.0-beta), because Maven orders a prerelease BEFORE its release; 26.2.1-alpha is
# the earliest possible 26.2.1-* qualifier, so this bound excludes them all.
# 26.2 NeoForge betas have broken client API between builds before (e.g.
# RenderArmEvent.getPlayer() was removed after 26.2.0.6-beta), so re-verify client
# rendering before ever raising the CERTIFIED baseline past .25.
neo_version_range=[26.2.0.12-beta,26.2.1-alpha)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=iamzombieq
# The human-readable display name for the mod.
mod_name=I Am Zombie?
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=1.1.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=dev.molang.iamzombieq
